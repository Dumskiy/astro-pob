---
const { name, width, height, class:className = '', ...rest } = Astro.props;
---

<svg class={className} width={width} height={height} aria-hidden="true" {...rest}>
  <use href={`#${name}`} />
</svg>
