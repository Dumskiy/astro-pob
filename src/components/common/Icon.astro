---
const { name, width, height, size, class: className = '', ...rest } = Astro.props;
---

<svg class={className} width={size || width} height={size || height} aria-hidden="true" {...rest}>
  <use href={`#${name}`} />
</svg>
